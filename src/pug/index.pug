include ./includes




<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        +header
        main.page__main
            +title('ЗИМБАБВЕ')
            +travel-cards
            +request-tour
        .test-conteiner
            +dropdown(dropdownTest)
            +date-dropdown()